README.md
README.ko.md
Brewfile
Brewfile.ai-cli-tools
Brewfile.macos-desktop-apps
AGENTS.md
CONTEXT.md
install.sh
docs/**
.github/**
tests/**

{{ if hasKey . "terrapod" }}
# The Go manager installs and owns its stable launcher pair.
.local/bin/terrapod
.local/bin/tpod
{{ end }}

# cmux settings are intentionally unmanaged; existing local settings are left untouched.
.config/cmux
.config/cmux/**

{{ if or (not (hasKey . "terrapod")) (not (or (default false .terrapod.enableEditorStack) (default false .terrapod.enableDevelopmentWorkspace))) }}
# Optional Editor Stack entries are opt-in for every machine profile.
.config/nvim
.config/nvim/**
{{ end }}

{{ if or (not (hasKey . "terrapod")) (not (default false .terrapod.enableDevelopmentWorkspace)) }}
# Optional Development Workspace entries are opt-in for every machine profile.
.config/zellij/layouts
.config/zellij/layouts/**
{{ end }}

{{ if ne .chezmoi.os "darwin" }}
# macOS Terminal Profile entries are not part of the VPS Shell Profile.
.config/ghostty
.config/ghostty/**
.config/karabiner
.config/karabiner/**
.hammerspoon
.hammerspoon/**
.zprofile
{{ end }}
